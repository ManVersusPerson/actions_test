name: action_tester

on: push

jobs:
  super-lint:
    name: Lint code base
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - run: pwd
      - run: ls -lah 
      - run: pip -V
      - run: python --version
      - run: wget https://github.com/ManVersusPerson/download_to_action/archive/refs/heads/main.zip
      - run: pwd
      - run: ls -lah
      - run: unzip main.zip
      - run: |
          cd download_to_action-main
          ls -lah
          cd ..
          ls -lah
